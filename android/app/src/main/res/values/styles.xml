<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--
      windowBackground forcé en bleu marine foncé (couleur de fond de l'app en mode sombre,
      voir la variable de fond dans index.css) : en plein écran edge-to-edge, la WebView ne
      peint pas forcément la toute première frame sous la zone de la barre de statut masquée,
      sans ce réglage c'est le gris par défaut de DayNight (résolu clair/sombre selon le
      thème système) qui apparaît à cet endroit au lieu du fond de l'app.
    -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">#0b1620</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>